{
  fn isEven(n) {
    if (n == 0) { return true; }
    # [line 5] Error at 'isOdd': Cannot find value with this name in current scope.
    return isOdd(n - 1);
  }

  fn isOdd(n) {
    if (n == 0) { return false; }
    return isEven(n - 1);
  }

  isEven(4);
}
